# The canonical origin. Used for canonical URLs, OpenGraph tags, the sitemap
# and robots.txt. Vite inlines VITE_* variables at build time, so this must be
# set in the build environment — not at runtime in the container.
VITE_SITE_URL=https://swornimsanjel.com
