# The canonical origin. Used for metadataBase, OpenGraph URLs, the sitemap
# and robots.txt. Set this before the first production deploy — see the
# open question in README.
NEXT_PUBLIC_SITE_URL=https://swornim.avernek.com
